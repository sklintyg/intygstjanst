<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:batch="http://www.springframework.org/schema/batch"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
          http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.1.xsd
          http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
          http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<beans:description>Certificate info model migration jobs</beans:description>

	<batch:job id="certificateInfoModelMigrationJob" incrementer="jobIncrementer">
		<batch:step id="certificateInfoModelProcessingStep.master">
			<batch:partition step="certificateInfoModelProcessingStep" partitioner="columnRangePartitioner" 
				handler="certificatePartitioningHandler" />
		</batch:step>
	</batch:job>

	<beans:bean id="jobIncrementer"
		class="org.springframework.batch.core.launch.support.RunIdIncrementer" />

	<batch:step id="certificateInfoModelProcessingStep">
		<batch:tasklet transaction-manager="certificateTransactionManager">
			<batch:chunk reader="certificateItemReader" processor="certificateCompositeItemProcessor"
				writer="certificateItemWriter" commit-interval="10" skip-limit="10">
				<batch:skippable-exception-classes>
					<batch:include class="se.inera.certificate.migration.processors.CertificateProcessingException"/>
				</batch:skippable-exception-classes>
			</batch:chunk>
			<batch:listeners>
				<batch:listener ref="certificateSkipListener" />
			</batch:listeners>	
		</batch:tasklet>
		<batch:fail on="FAILED" />
		<batch:end on="*" />
	</batch:step>

	<beans:bean id="certificateSkipListener" class="se.inera.certificate.migration.listeners.CertificateProcessingSkipListener" />

	<beans:bean id="certificatePartitioningHandler" class="org.springframework.batch.core.partition.support.TaskExecutorPartitionHandler">
		<beans:property name="taskExecutor" ref="certificateTaskExecutor" />
		<beans:property name="step" ref="certificateInfoModelProcessingStep" />
		<beans:property name="gridSize" value="5" />
	</beans:bean>
	
	<beans:bean id="columnRangePartitioner" class="se.inera.certificate.migration.partition.ColumnRangePartitioner">
		<beans:property name="table" value="original_certificate" />
		<beans:property name="column" value="id" />
		<beans:property name="dataSource" ref="certificateDataSource" />
	</beans:bean>
	
	<beans:bean id="certificateTaskExecutor" class="org.springframework.core.task.SimpleAsyncTaskExecutor"/>

</beans:beans>