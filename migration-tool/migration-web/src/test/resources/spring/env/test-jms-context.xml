<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
  xmlns:context="http://www.springframework.org/schema/context"
  xmlns:jms="http://www.springframework.org/schema/jms" xmlns:amq="http://activemq.apache.org/schema/core"
  xsi:schemaLocation="http://www.springframework.org/schema/beans 
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context 
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/jms 
                           http://www.springframework.org/schema/jms/spring-jms.xsd
                           http://activemq.apache.org/schema/core 
                           http://activemq.apache.org/schema/core/activemq-core.xsd"
  profile="dev">

  <description>Activates and configures an embedded ActiveMQ JMS broker
    for use in the DEV environment
  </description>

  <!-- Embedded ActiveMQ Broker -->
  <amq:broker id="amqBroker" useJmx="true" persistent="false">
    <amq:transportConnectors>
      <amq:transportConnector uri="tcp://localhost:0" />
    </amq:transportConnectors>
  </amq:broker>

  <!-- ActiveMQ Destination -->
  <amq:queue id="statsDestination" physicalName="statistik.utlatande.queue" />

  <!-- JMS ConnectionFactory to use, configuring the embedded broker using 
    XML -->
  <amq:connectionFactory id="jmsFactory"
    brokerURL="vm://localhost" />

  <!-- JMS Producer Configuration -->
  <bean id="jmsProducerConnectionFactory"
    class="org.springframework.jms.connection.SingleConnectionFactory"
    depends-on="amqBroker" p:targetConnectionFactory-ref="jmsFactory" />

  <beans profile="unit-testing">

    <description>JMX beans for use with the embedded broker in unit
      testing</description>

    <!-- Will be injected in tests using auto-wire -->
    <bean id="jmsProducerTemplate" class="org.springframework.jms.core.JmsTemplate"
      p:connectionFactory-ref="jmsProducerConnectionFactory"
      p:defaultDestination-ref="statsDestination" />

    <bean id="mBeanConnection"
      class="org.springframework.jmx.support.MBeanServerConnectionFactoryBean"
      p:serviceUrl="service:jmx:rmi:///jndi/rmi://localhost:1099/jmxrmi" />

    <bean id="testQueueInspector"
      class="se.inera.certificate.migration.testutils.jms.TestQueueInspector"
      p:mBeanConn-ref="mBeanConnection" />
  </beans>

</beans>