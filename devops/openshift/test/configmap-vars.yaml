apiVersion: v1
data:
  SPRING_PROFILES_ACTIVE: "test,testability-api,caching-enabled,it-fk-stub,wc-hsa-stub,it-ts-stub"
  INTYGSTJANST_BASE_URL:  "http://${app.name}:8080/inera-certificate"
  NTJP_BASE_URL: "http://${app.name}:8080/inera-certificate/stubs"
  NTJP_WS_CERTIFICATE_TYPE: JKS
  NTJP_WS_TRUSTSTORE_TYPE: JKS
kind: ConfigMap
metadata:
  name: intygstjanst-test-configmap-envvar
type: Opaque
