<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>se.inera.intyg</groupId>
    <artifactId>intyg-parent</artifactId>
    <version>1.3-SNAPSHOT</version>
  </parent>

  <groupId>se.inera.certificate</groupId>
  <artifactId>certificate</artifactId>
  <packaging>pom</packaging>
  <name>Inera Certificate</name>

  <description>
    Inera Certificate Root Project - The parent of all other Inera Certificate modules.
  </description>

  <properties>
    <!-- Project properties -->
    <repository.host>repository-callistasoftware.forge.cloudbees.com</repository.host>
    <logback-util.version>1.1.2</logback-util.version>
    <checkstyle.basedir>${project.basedir}</checkstyle.basedir>

    <!-- Properties for project modules having their own versioning strategy -->
    <fk7263.version>1.0-SNAPSHOT</fk7263.version>
    <ts-bas.version>1.0-SNAPSHOT</ts-bas.version>
    <ts-diabetes.version>1.0-SNAPSHOT</ts-diabetes.version>
    <liquibase-runner.version>1.0-SNAPSHOT</liquibase-runner.version>
  </properties>

  <modules>
    <module>schemas</module>
    <module>module-api</module>
    <module>web</module>
    <module>specifications</module>
    <module>persistence</module>
    <module>tools/LiquibaseRunner</module>
    <module>tools/LiquibaseRunnerForCertificate</module>
    <!-- <module>tools/AnonymiseraIntygsDatabas</module> -->
  </modules>

  <scm>
    <connection>scm:git:https://${scm.host}/p/inera-certificate/</connection>
    <tag>HEAD</tag>
  </scm>

  <dependencyManagement>
    <dependencies>
      <!-- Project dependencies -->

      <!-- TODO: Finns den här verkligen? -->
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>model</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>liquibase-runner</artifactId>
        <version>${liquibase-runner.version}</version>
      </dependency>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>logback-util</artifactId>
        <version>${logback-util.version}</version>
      </dependency>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>module-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>certificate-persistence</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>certificate-schemas</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Dependencies to Intygstjansten's modules project -->
      <dependency>
        <groupId>se.inera.certificate.modules</groupId>
        <artifactId>module-support</artifactId>
        <version>${project.parent.version}</version>
      </dependency>
      <dependency>
        <groupId>se.inera.certificate.modules</groupId>
        <artifactId>fk7263</artifactId>
        <version>${fk7263.version}</version>
      </dependency>
      <dependency>
        <groupId>se.inera.certificate.modules</groupId>
        <artifactId>ts-bas</artifactId>
        <version>${ts-bas.version}</version>
      </dependency>
      <dependency>
        <groupId>se.inera.certificate.modules</groupId>
        <artifactId>ts-diabetes</artifactId>
        <version>${ts-diabetes.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>

    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <repositories>
    <repository>
      <id>callista-release</id>
      <url>https://${repository.host}/release/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>callista-snapshot</id>
      <url>https://${repository.host}/snapshot/</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>thirdparty</id>
      <url>https://${repository.host}/3rdparty</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

</project>
 
